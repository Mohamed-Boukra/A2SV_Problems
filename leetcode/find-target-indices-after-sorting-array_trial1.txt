class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        count = nums.count(target)
        start = len(nums) - nums.count(target) - sum(x > target for x in nums)
        return list(range(start, start + count))