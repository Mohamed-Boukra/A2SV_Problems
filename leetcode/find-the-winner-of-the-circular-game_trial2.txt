class Solution:
    def findTheWinner(self, n: int, k: int) -> int:
        friends = list(range(1, n + 1))
        curr= 0

        while len(friends) > 1:
            curr = (curr+ k-1) % len(friends)
            friends.pop(curr)

        return friends[0]
        