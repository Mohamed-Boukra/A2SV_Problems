class StockSpanner:

    def __init__(self):
        self.q = deque()

    def next(self, price: int) -> int:
        span = 1
        while self.q and self.q[-1][0] <= price:
            span += self.q.pop()[1]
        self.q.append((price,span))
        return span

# Your StockSpanner object will be instantiated and called as such:
# obj = StockSpanner()
# param_1 = obj.next(price)