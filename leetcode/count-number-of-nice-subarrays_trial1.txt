class Solution:
    def numberOfSubarrays(self, nums: List[int], k: int) -> int:
        count = defaultdict(int)
        count[0] = 1
        prefix = 0
        res = 0
        for x in nums:
            prefix += x % 2
            res += count[prefix - k]
            count[prefix] += 1
        return res