class Solution:
    def reductionOperations(self, nums: List[int]) -> int:
        nums.sort()
        res = 0
        levels = 0
        for i in range(1, len(nums)):
            if nums[i] != nums[i-1]:
                levels += 1
            res += levels
        return res